[mail]
sendmail=smtp
; 	options
;      0 = disabled
;      1 = use default
;      mail=use php mail() function (supported in PHPMailer)
;      smtp=use smtp settings
;
; debug=2 (supported by PHPMailer)
; basedir= default '' (prepend image urls supported by PHPMailer)

; SMTP settings
host='mail.example.com'
; port= default 25
auth=1
username=testuser
password=testpwd

[mail-lists]
list1=terry@foo.com,webmaster

[email]
webmaster=web@master.com

